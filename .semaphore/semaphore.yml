version: v1.0
name: Test MyApp
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Test
    task:
      jobs:
        - name: Test
          commands:
            - checkout
            - cache restore
            - composer install
            - cache store
            - composer update
            - cp .env.example .env
            - 'php artisan key:generate'
            - 'php artisan config:cache'
            - php artisan migrate
            - ./vendor/bin/phpunit
      env_vars:
        - name: APP_ENV
          value: local
        - name: DB_DATABASE
          value: ecommerce
